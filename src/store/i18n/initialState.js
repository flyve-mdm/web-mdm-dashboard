/**
 * Inital State for I18N:
 * - Language per default
 * - Language current rendering
 */

const initialState = {
  languageDefault: 'en_GB',
  languageCurrent: 'en_GB'
}

export default initialState
