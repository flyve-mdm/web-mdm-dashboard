import EmptyMessage from '../../components/EmptyMessage'

const routes = [
    {
        path: '/',
        name: 'No selected',
        component: EmptyMessage,
        exact: true
    }
]

export default routes