before_script:
  - npm rebuild node-sass
  - yarn install

stages:
  - deploy

deployment:
  stage: deploy
  script:
    - yarn build
    - rm -r /var/www/public/dashboard
    - cp -r ./build /var/www/public/dashboard
  only:
    - develop
    
